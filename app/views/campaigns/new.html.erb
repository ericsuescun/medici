<div class="container mt-3">
  <h1><%= t("campaigns.new_title") %></h1>
  <p class="text-muted"><%= @study.public_title %></p>
  <%= render "form", campaign: @campaign, form_url: study_campaigns_path(@study) %>
  <div class="mt-2">
    <%= link_to t("common.back"), study_campaigns_path(@study) %>
  </div>
</div>
