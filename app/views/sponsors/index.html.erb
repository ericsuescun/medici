<div class="container py-4">
  <div class="row">
    <div class="col-12">
      <% if notice.present? %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>

      <% content_for :title, t("sponsors.index_title") %>

      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0"><%= t("sponsors.index_title") %></h1>
        <div>
          <%= link_to t("sponsors.back_to_home"), static_pages_medici_home_path %> |
          <%= link_to t("sponsors.new_sponsor"), new_sponsor_path%>
        </div>
      </div>

      <div id="sponsors">
        <div class="card mb-4">
          <div class="card-body">
            <div class="table">
              <table class="table table-striped">
                <thead>
                <tr>
                  <th><%= Sponsor.human_attribute_name(:name) %></th>
                  <th><%= t("sponsors.studies_count") %></th>
                  <th><%= Sponsor.human_attribute_name(:initials) %></th>
                  <th><%= Sponsor.human_attribute_name(:sponsor_type) %></th>
                </tr>
                </thead>
                <tbody>
                <% @sponsors.each do |sponsor| %>
                  <tr>
                    <td><%= link_to sponsor.name, sponsor %></td>
                    <td><span class="badge bg-primary"><%= sponsor.studies.count %></span></td>
                    <td><%= sponsor.capitals %></td>
                    <td><%= t_enum(sponsor, :sponsor_type) %></td>
                  </tr>
                <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
        
        <div class="d-flex justify-content-between align-items-center">
          <%= will_paginate @sponsors, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer %>
          <div>
            <%= link_to t("sponsors.back_to_home"), static_pages_medici_home_path %> |
            <%= link_to t("sponsors.new_sponsor"), new_sponsor_path%>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
