<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <% title = t("studies.new_study_typed", type: t("studies.types.#{@study.study_type}")) %>
      <% content_for :title, title %>

      <h1><%= title %></h1>

      <%= render "form", study: @study %>

      <br>

      <div>
        <%= link_to t("studies.return_to_studies"), studies_path %>
      </div>
    </div>
  </div>
</div>
