<% content_for :title, t("articles.edit_title") %>

<h1><%= t("articles.edit_title") %></h1>

<%= render "form", article: @article %>

<br>

<div>
  <%= link_to t("articles.show_this"), @article %> |
  <%= link_to t("articles.back_to_list"), articles_path %>
</div>
