<div class="container-fluid mt-3">
  <div class="row">
    <div class="col-4 mx-auto">
      <% content_for :title, t("articles.new") %>
      <h1><%= t("articles.new") %></h1>
      <%= render "form", article: @article %>
      <div class="mt-2">
        <%= link_to t("articles.back_to_list"), articles_path %>
      </div>
    </div>
  </div>
</div>
