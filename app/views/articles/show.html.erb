<p style="color: green"><%= notice %></p>

<%= render @article %>

<div>
  <%= link_to t("articles.edit_this"), edit_article_path(@article) %> |
  <%= link_to t("articles.back_to_list"), articles_path %>

  <%= button_to t("articles.delete_this"), @article, method: :delete %>
</div>
