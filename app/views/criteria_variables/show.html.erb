<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <p style="color: green"><%= notice %></p>

      <%= render @criteria_variable %>

      <div class="mt-3">
        <%= link_to t("common.edit"), edit_criteria_profile_criteria_variable_path(@criteria_profile, @criteria_variable) %> |
        <%= link_to t("criteria_variables.back_to_variables"), criteria_profile_criteria_variables_path(@criteria_profile) %>
        <% if current_user&.admin? %>
          | <%= link_to t("criteria_variables.change_control"), change_control_path("CriteriaVariable", @criteria_variable.id) %>
        <% end %>

        <%= button_to t("common.delete"), [@criteria_profile, @criteria_variable], method: :delete, class: "btn btn-primary btn-block mt-3 mb-3" %>
      </div>
    </div>
  </div>
</div>
