<div class="container-fluid">
  <div class="row">
    <div class="col-4 mx-auto">
      <% content_for :title, t("cities.edit_title") %>

      <h1><%= t("cities.edit_title") %></h1>

      <%= render "form", city: @city %>

      <br>

      <div>
        <%= link_to t("common.show"), @city %> |
        <%= link_to t("cities.back_to_list"), cities_path %>
      </div>
    </div>
  </div>
</div>
