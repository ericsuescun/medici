<div class="container-fluid">
  <div class="row">
    <div class="col-4 mx-auto">
      <% content_for :title, t("cities.new") %>

      <h1><%= t("cities.new") %></h1>

      <%= render "form", city: @city %>

      <br>

      <div>
        <%= link_to t("cities.back_to_list"), cities_path %>
      </div>
    </div>
  </div>
</div>
