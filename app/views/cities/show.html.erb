<div class="container-fluid">
  <div class="row">
    <div class="col-4 mx-auto">
      <p style="color: green"><%= notice %></p>

      <%= render @city %>

      <div>
        <%= link_to "Editar", edit_city_path(@city) %> |
        <%= link_to "Retornar a ciudades", cities_path %>
      </div>
    </div>
  </div>
</div>
