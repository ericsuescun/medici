<div class="container">
  <div class="row">
    <div class="col-12 mx-auto">
      <p style="color: green"><%= notice %></p>

      <% content_for :title, "Ciudades" %>
      <%= link_to "Nueva ciudad", new_city_path %> |
      <%= link_to "Retornar a Medici", static_pages_medici_home_path %>
      <h1>Ciudades</h1>


        <div class="row">
            <% @cities.each do |city| %>

            <div class="col-3">
              <%= render partial: 'city', locals: { city: city } %>
            </div>

            <% end %>
        </div>
      <%= link_to "Nueva ciudad", new_city_path %> |
      <%= link_to "Retornar a Vitalia", static_pages_medici_home_path %>
    </div>
  </div>
</div>
