<%= form_with(model: trial_city) do |form| %>
  <% if trial_city.errors.any? %>
    <div style="color: red">
      <h2><%= t("trial_cities.form.error_header", count: trial_city.errors.count) %></h2>

      <ul>
        <% trial_city.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :studies_id, style: "display: block" %>
    <%= form.text_field :studies_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
