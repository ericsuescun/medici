<p style="color: green"><%= notice %></p>

<% content_for :title, t("trial_cities.index_title") %>

<h1><%= t("trial_cities.index_title") %></h1>

<div id="trial_cities">
  <% @trial_cities.each do |trial_city| %>
    <%= render trial_city %>
    <p>
      <%= link_to t("common.show"), trial_city %>
    </p>
  <% end %>
</div>

<%= link_to t("trial_cities.new_title"), new_trial_city_path %>
