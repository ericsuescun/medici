<p style="color: green"><%= notice %></p>

<%= render @contact %>

<div>
  <%= link_to t("contacts.edit_this"), edit_contact_path(@contact) %> |
  <%= link_to t("contacts.back_to_list"), contacts_path %>

  <%= button_to t("contacts.delete_this"), @contact, method: :delete %>
</div>
