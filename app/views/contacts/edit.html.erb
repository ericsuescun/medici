<% content_for :title, t("contacts.edit_title") %>

<h1><%= t("contacts.edit_title") %></h1>

<%= render "form", contact: @contact %>

<br>

<div>
  <%= link_to t("contacts.show_this"), @contact %> |
  <%= link_to t("contacts.back_to_list"), contacts_path %>
</div>
