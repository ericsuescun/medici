<div class="container-fluid">
  <div class="row">
    <div class="col-4 mx-auto">
      <% content_for :title, t("patients.new_patient") %>

      <h1><%= t("patients.new_patient") %></h1>

      <%= render "form", patient: @patient %>

      <br>

      <div>
        <%= link_to t("patients.back_to_index"), patients_path %>
      </div>
    </div>
  </div>
</div>
