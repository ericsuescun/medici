<div class="container-fluid">
  <div class="row">
    <div class="col-4 mx-auto">
      <% content_for :title, t("patients.edit_title") %>

      <h1><%= t("patients.edit_title") %></h1>

      <%= render "form", patient: @patient %>

      <br>

      <div>
        <%= link_to t("common.show"), @patient %> |
        <%= link_to t("patients.back_to_index"), patients_path %>
      </div>
    </div>
  </div>
</div>
